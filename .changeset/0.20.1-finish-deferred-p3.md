---
'@bookedsolid/rea': patch
---

Close five P3-tier items deferred from the 0.19.0 3-agent review.

The user's directive was clear: do EVERYTHING, no deferrals. 0.19.0
shipped Critical + P1 + P2 findings closed in-tree, but five P3-tier
items got punted. This patch closes them.

### P3-3 (code-reviewer) — cache-hit emits EVT_REVIEWED with `cache_hit: true`

Pre-fix the cache-hit branch in `runPushGate` emitted `EVT_CACHE_HIT`
only — operators grepping `rea.push_gate.reviewed` for verdict-stability
dashboards (the canonical pattern) would miss every cached push.
0.20.1 emits BOTH events on cache hit: `EVT_CACHE_HIT` for forensic
detail on the cache layer, AND `EVT_REVIEWED` with `cache_hit: true`
metadata so the canonical reviewed-event grep sees every push.

The audit-emission cross-file contract test is extended to pin the
dual-emit + the `cache_hit: true` discriminator so a future refactor
that drops one fails CI.

### P3-1 (backend-engineer) — cache-hit return-shape simplification

The cache-hit branch's `status` field used a nested ternary on
`cachedBlocked` that re-derived the same verdict mapping in both arms.
Replaced with the obvious 1:1 mapping — `status: cached.verdict;
exitCode: cachedBlocked ? 2 : 0`. Correctness-equivalent; future
maintainers can read it.

### P2-3 (backend-engineer) — cache opportunistic prune at >500 entries

Pre-fix `pruneOlderThan` only ran when an external caller invoked it.
A long-lived consumer accumulating months of pushes against many
SHAs grew the cache file unbounded.

0.20.1 adds opportunistic prune in `writeVerdict`: when the merged
entry count exceeds `VERDICT_CACHE_PRUNE_THRESHOLD` (500), drop
entries whose own `ttl_ms` has expired before writing. The prune is
best-effort — if every entry is unexpired the file grows past 500
rather than dropping fresh durable verdicts.

### P3-4 (code-reviewer) — settings-merge consumer-state coverage

Pre-fix `settings-schema.test.ts` only tested
`mergeSettings({}, defaults)`. The merge layer is most likely to
drift on consumer upgrades — a 0.13.x-shaped settings.json that
already contains user-authored hooks must merge cleanly with the
latest defaults and produce a schema-valid document.

New test seeded with a synthetic 0.13.x consumer doc (model field,
env block, user-authored Bash + UserPromptSubmit hooks). Validates:
the merged document passes the Ajv schema, the user's
UserPromptSubmit hook survives (rea doesn't manage that event),
top-level `model` and `env` pass through unchanged.

### Test coverage

- 1299 vitest tests pass (was 1297 in 0.19.0), +2 fixtures
- All 6 quality gates green
- Cross-file contract test now pins the dual-emit + discriminator —
  future refactor that drops either fails CI

### Deferred from 0.20.1 (still open, called out in 0.19.0 changeset)

- npm CDN verify-step flake — `.github/workflows/release.yml` change
  pending policy approval (the file is in `policy.blocked_paths`)
- Hooks-as-Node-binary rewrite — long-tracked architectural item from
  helix cycle 016, multi-release effort
